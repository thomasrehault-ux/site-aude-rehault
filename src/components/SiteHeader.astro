---
const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, "");
const withBase = (path: string) => (path === "/" ? `${baseUrl}/` || "/" : `${baseUrl}${path}`);

const navigation = [
  { href: "#publics", label: "À qui ?" },
  { href: "#approche", label: "Approche" },
  { href: "#praticienne", label: "Praticienne" },
  { href: "#seances", label: "Séances" },
  { href: "#contact", label: "Contact" }
];
---

<header class="site-header">
  <a class="brand" href={withBase("/")}>
    <span class="brand-title">Aude Réhault</span>
    <span class="brand-subtitle">Psychologue clinicienne</span>
  </a>

  <nav class="site-nav" aria-label="Navigation principale">
    {
      navigation.map((item) => (
        <a href={item.href}>
          {item.label}
        </a>
      ))
    }
  </nav>
</header>
